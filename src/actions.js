import * as types from './types'

export const incrementGrayscale = () => ({ type: types.INCREMENT })

export const decrementGrayscale = () => ({ type: types.DECREMENT })

export const resetGrayscale = () => ({ type: types.RESET })